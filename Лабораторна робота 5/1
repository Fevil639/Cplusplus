#include <iostream>
#include <cmath> 

using namespace std;

int main() {
    double x, y;

    cout << "Введіть значення x: ";
    cin >> x;


    if (x >= 7) {
        y = 8 + sqrt(x - 7) / (x + 5);
        cout << "y = " << y << endl;
    } else {
        cout << "Помилка: підкореневий вираз повинен бути >= 0." << endl;
    }

    return 0;
}
