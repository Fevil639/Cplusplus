#include <iostream>             // Підключаємо бібліотеку для виводу/вводу в консоль
using namespace std;           // Дозволяє не писати std:: перед cout, endl і т.д.

                              // 🔹 Функція isLeap перевіряє, чи рік високосний
bool isLeap(int year) {
                // Умови високосного року:
               // - Ділиться на 4 — можливо високосний
              // - Якщо також ділиться на 100 — НЕ високосний
             // - Але якщо ділиться на 400 — ТОЧНО високосний

    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
          // year % 4 == 0 — перевірка, чи рік ділиться на 4
         // year % 100 != 0 — не має ділитися на 100
        // year % 400 == 0 — виняток: якщо ділиться на 400 — високосний
}

          // 🔹 Точка входу в програму — головна функція
int main(void) {
          // Цикл перевіряє кожен рік від 1995 до 2016 включно
    for(int yr = 1995; yr < 2017; yr++) {
        // Виводимо рік і результат функції isLeap:
        // 1 — високосний, 0 — не високосний
        cout << yr << " -> " << isLeap(yr) << endl;
    }

    return 0; // Завершення програми
}
